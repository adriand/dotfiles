#!/usr/bin/env zsh

for file in $*; do
  basename="$(basename "$file")"

  rsync -rve ssh --progress "$1" roughsleeper:"\"/srv/www/nilved.me/public/$basename\"" >&2

  echo "http://nilved.me/$basename"
done
