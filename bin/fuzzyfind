#!/usr/bin/env python2
from fuzzywuzzy import process

def main(stdin, argv):
    files = stdin.read().splitlines()
    query = argv.pop()

    matches = process.extractOne(query, files)
    most_likely = matches[0]

    print most_likely

if __name__ == "__main__":
    import sys

    main(sys.stdin, sys.argv)
