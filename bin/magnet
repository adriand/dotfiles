#!/usr/bin/env bash

[[ "$1" =~ xt=urn:btih:([^&/]+) ]] || exit 1;

cd "/tmp"

torrent="meta-${BASH_REMATCH[1]}.torrent"
echo "d10:magnet-uri${#1}:${1}e" > "$torrent"

rsync -rve ssh --progress\
  "$torrent"\
  nilved@beige.feralhosting.com:"\"private/rtorrent/watch/\""
