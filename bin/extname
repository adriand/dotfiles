#!/usr/bin/env python

def main(argv):
  for filename in argv[1:]:
    split = filename.split(".")
    first_part = split.pop(0)
    the_rest = ".".join(split)

    print(the_rest)

if __name__ == "__main__":
  import sys

  main(sys.argv)
